import styled from "styled-components";

export default function Brand() {
    return <StyledTitle>RepoProvas</StyledTitle>;
  }

  const StyledTitle = styled.h1`
  margin: 32px 0 0 32px;
  font-family: "Satisfy", cursive;
  font-size: 32px;
  color: #f7f7f7;
`;